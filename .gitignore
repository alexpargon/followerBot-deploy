# Solo para evitar accidentes — este repo no debería contener nada de esto:
.env
*.session
*.session-journal
trades.db
*.log
__pycache__/
# MQL5 SOURCE must NEVER be committed to this public repo — only compiled .ex5.
*.mq5
*.mqh
